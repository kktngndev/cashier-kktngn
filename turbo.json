{
  "$schema": "https://turbo.build/schema.json",
  "globalDotEnv": [".env.local", ".env"],
  "globalDependencies": [".env", "tsconfig.json"],
  "pipeline": {
    "lint": {},
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "node_modules/**", "tsconfig.json"],
      "dependsOn": ["^build"]
    },
    "type-check": {}
  }
}